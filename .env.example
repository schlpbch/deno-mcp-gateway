# Environment Variables for deno Federated MCP Gateway

# Backend Server URLs
# Optional: Configure servers via environment variables
# Or use the upload-config script to register servers dynamically
# JOURNEY_SERVICE_URL=http://your-server-url/mcp
# SWISS_MOBILITY_URL=http://your-server-url/mcp
# AAREGURU_URL=http://your-server-url/mcp
# OPEN_METEO_URL=http://your-server-url/mcp

# Cache Configuration
CACHE_TTL=300
CACHE_MAX_SIZE=10000

# Retry Configuration
RETRY_MAX_ATTEMPTS=3
RETRY_BACKOFF_DELAY=100
RETRY_BACKOFF_MULTIPLIER=2.0
RETRY_MAX_DELAY=2000

# Timeout Configuration (milliseconds)
TIMEOUT_CONNECT=5000
TIMEOUT_READ=30000

# Health Check Configuration
HEALTH_CHECK_INTERVAL=60000
HEALTH_UNHEALTHY_THRESHOLD=3
